// Shared between FormField and SelectField -- identical chrome for both,
// so the two never quietly drift apart from each other.
export const FIELD_CHROME_CLASSNAME =
  "min-h-11 rounded-lg border border-border/15 bg-surface-subtle px-3 py-2.5 text-base text-text-primary outline-none transition-colors focus:border-green-500 focus:ring-2 focus:ring-green-500/30";
