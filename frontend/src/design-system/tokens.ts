/**
 * Placeholder design tokens. Real values (and the full type scale, spacing
 * scale, and motion durations from spec §3) land in the M5 design-system
 * milestone — this file exists now so Tailwind config and components have
 * exactly one place to import from once that work starts.
 */
export const motion = {
  durationFastMs: 150,
  durationBaseMs: 250,
} as const;
